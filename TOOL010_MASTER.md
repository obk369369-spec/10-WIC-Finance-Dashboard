# TOOL010 MASTER — 10번 재정·재무 관리 프로그램

상태: ACTIVE / TOOL-SPECIFIC SINGLE SOURCE
기준일: 2026-08-30

이 문서는 10번 도구 고유 규칙만 보관한다. WIC 공통 운영규칙은 중앙 글로벌 원본을 우선하며 공통 규칙을 중복 복제하지 않는다.

## 1. 목적
- 사업장이 거래처와 거래하면서 발생하는 수입·지출·미수금·미지급금 등 재정·재무 상태를 한 화면에서 기록하고 확인한다.
- 단순 회계 계산기가 아니라 거래처별·기간별 흐름과 현재 정산 상태를 빠르게 볼 수 있는 운영 대시보드다.

## 2. 거래 입력
최소 필수:
- 날짜
- 거래처
- 구분(수입/지출)
- 금액

선택/관리 항목:
- 내역/메모
- 분류
- 지급수단
- 정산상태(완료/미수금/미지급금)
- 부가세 등 실제 업무에서 필요한 세무 표시

입력값이 부족하면 임의 거래를 만들지 않는다.

## 3. 조회/요약
- 기간, 거래처, 수입/지출 등 필터를 적용한다.
- 필터 기준 총수입, 총지출, 순이익, 미수금, 미지급금을 계산한다.
- 거래 목록, 거래처별 요약, 월별 요약을 구분해 볼 수 있게 한다.
- 같은 필터가 요약/표/차트에 일관되게 적용되어야 한다.

## 4. 시각화
- 월별 수입/지출, 지출 분류 비중, 누적 잔액 등 재무 흐름을 시각화할 수 있다.
- 차트와 숫자 요약이 서로 다른 값을 보여주면 FAIL이다.
- 데이터가 없으면 가짜 차트를 만들지 않고 데이터 없음으로 표시한다.

## 5. 데이터 보존/정합성
- 거래 1건은 한 레코드로 유지하고 다른 거래처/날짜/금액을 섞지 않는다.
- 삭제, 수정, 필터 처리 후 KPI·표·차트가 모두 같은 상태를 반영해야 한다.
- 실제 운영본에서는 새로고침/재실행 시 데이터 보존 요구를 명확히 검증한다. 저장 기능이 없으면 있다고 표시하지 않는다.
- 백업/내보내기 기능을 붙일 경우 실제 복구까지 검증해야 한다.

## 6. 단순 사용 흐름
- 거래 입력 → 추가 → 요약/목록 확인 → 필요 시 필터의 흐름을 기본으로 한다.
- 화면에 없는 숨은 재무 기능을 완료 기능처럼 취급하지 않는다.
- 사용자에게 복잡한 파일/코드 관리를 요구하지 않는다.

## 7. 버전/확장
- 과거 `v1.2`, `v1.3` 등의 임시 단계명은 영구 규칙이 아니다.
- 부가세·세금계산서·회계 내보내기·연동 등 확장 기능은 실제 필요와 검증 범위에 따라 추가한다.
- 확장 기능 때문에 이미 PASS된 기본 거래/KPI 흐름을 다시 만들지 않는다.

## 8. 검증
- 새 거래 추가, 수입/지출 계산, 미수/미지급 계산, 필터, 삭제/수정, 차트/표 정합성을 변경 범위 기준으로 확인한다.
- 새 변경은 FIRST_VALIDATION 1회, 기존 PASS 영역은 SKIP_REUSE한다.
- 대화창에서 실제로 돌지 않는 자동 테스트를 계속 수행 중이라고 보고하지 않는다.
- 실제 브라우저 실행/데이터 상태 증거 없이 `0% 남음/완료`를 승계하지 않는다.

## 9. 2026-08-30 대화기록 catch-up
- 제공된 10번 과거 기록에서 지속 적용할 재정·재무 기능과 정합성 규칙만 통합했다.
- 과거 가상 진행률·백그라운드 검증·임시 버전 완료 주장은 현재 완료 증거로 복원하지 않는다.
- 이후 10번 대화창에서 사용자가 `업데이트`라고 입력하면 신규 영구 피드백만 이 MASTER와 대조해 DIFF ONLY 반영한다.
- 같은 범위 충돌은 더 최신의 명시적 사용자 지시를 우선한다.
- 실제 GitHub write/commit/read-back 없이 업데이트 완료라고 보고하지 않는다.
